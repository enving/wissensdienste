# FastMCP brings its own Starlette/uvicorn stack for the streamable-HTTP transport.
# Pinned to the 3.x line validated here (server.py uses the FastMCP core API:
# @mcp.tool, @mcp.custom_route, mcp.run(transport="http"), instructions=...).
fastmcp>=3.4,<4
